{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true,
    "allowImportingTsExtensions": true
  },
  "include": ["src"],
  "comment": "*.figma.ts(x) are @figma/code-connect templates (published via the figma CLI, not app code) — excluded so tsc doesn't try to resolve the 'figma' template runtime.",
  "exclude": ["**/*.figma.ts", "**/*.figma.tsx"]
}
